<%= form_with(model: mahesh) do |form| %>
  <% if mahesh.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(mahesh.errors.count, "error") %> prohibited this mahesh from being saved:</h2>

      <ul>
        <% mahesh.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :age %>
    <%= form.text_field :age %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
